using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoError : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hi There");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
